# Keep line endings sane for anyone who clones this on a different setup.
* text=auto

# PowerShell on Windows is happiest with CRLF.
*.ps1 text eol=crlf
*.psd1 text eol=crlf
*.psm1 text eol=crlf

# Binaries - never let git touch these.
*.exe binary
*.ico binary
*.png binary
